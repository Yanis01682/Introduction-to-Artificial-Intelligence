# Corpus

The original assignment package references a course-provided cloud link for `语料库.zip`.

That corpus archive is not committed here because it is about 423 MB and exceeds GitHub's normal single-file limit. Use the cloud link in the assignment PDF if the full raw corpus is needed.
